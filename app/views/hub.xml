<Alloy>
    <Window class="container">
        <View id="header">
            <Label id="appName" text="Tviller"></Label>
        </View>
        <View id="body">
            <ListView id="channelsView">
                <ListSection id="toplist">
                    <HeaderView>
                        <View class="subtitle">
                            <Label class="subtitle_text" text="Popular Channels"></Label>
                        </View>
                    </HeaderView>
                </ListSection>
                <ListSection id="allChannels" class="subtitle" headerTitle="All channels">
                    <HeaderView>
                        <View class="subtitle">
                            <Label class="subtitle_text" text="All Channels"></Label>
                        </View>
                    </HeaderView>
                </ListSection>
            </ListView>
        </View>
        <Toolbar platform="ios">
            <Items>
                <Button id="send" title="test" />
                <FlexSpace/>
                <Button id="contact" systemButton="ORGANIZE" />
                <FlexSpace/>
                <Button id="contact" systemButton="CONTACT_ADD" />
                <FlexSpace/>
                <Button id="contact" systemButton="TRASH" />
                <FlexSpace/>
                <Button id="about" systemButton="INFO_DARK" />
            </Items>
        </Toolbar>
        <View id="footer" platform="android"></View>
    </Window>
</Alloy>
